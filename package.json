{
  "name": "momentum",
  "version": "1.0.0",
  "private": true,
  "description": "A polished frontend-only goal visualization dashboard for presentations.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js"
  }
}
